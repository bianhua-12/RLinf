# Agent Notes

Durable implementation decisions live here so experiment behavior can be
reconstructed from a named commit instead of transient chat context.

- `planned/`: accepted design, not yet implemented.
- `implemented/`: code and verification exist in the same commit.
- `deprecated/`: superseded decisions retained for provenance.

Each note records the problem, decision, research contract, rejected
alternatives, verification evidence, consequences, and concrete artifacts.
